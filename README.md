# AML Transaction Monitoring (Scikit-learn)

A Python + scikit-learn project for experimenting with Anti–Money Laundering (AML) detection using tabular transaction + KYC-style CSV data.  
Goal: clean/merge datasets, engineer features, train baseline models, and evaluate results.

---

## Tech Stack
- Python 3.10+ (3.11 is fine)
- scikit-learn
- pandas, numpy
- (optional) matplotlib / seaborn for plots
- (optional) jupyter for notebooks